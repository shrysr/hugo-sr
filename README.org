
* Introduction

This is the source of my personal website, based on hugo.

Visit my website at https://shreyas.ragavan.co

* Workflow

I use [[https://www.gnu.org/software/emacs/][Emacs]] for .. almost everything and use the excellent [[https://github.com/kaushalmodi/ox-hugo][ox-hugo]] package to
maintain the entire contents of my website in a single Org mode file. The
obvious advantage is the ability to work within Emacs, resulting in a friction
free writing and publishing experience.

This repository is pulled every 5 minutes through a cron job on my Linode VPS,
and the static website is served via Apache. The reason I use Apache instead of
the Hugo server, is that I also use the same VPS to serve multiple experimental
sites and wikis. Subsequent to the git pull, the Hugo server is started up to
build from this source repository and I've found it will only copy over updated or
modified files to the public directory served by Apache. Since Hugo generates
the entire site in milliseconds, I can get by with the cron job running every 5
minutes.

Anytime I want to create a blog post or an update, I work on a local hugo server
for previewing the post and the site, and once I commit and push - the cron job
described above will ensure the latest version of the site is deployed. I do not
edit any files of this site directly on the VPS.

It would actually be more efficient to use a service like Netlify for automated
deployment, which would add other benefits and make the process more
robust. That is.. for the future.

* Theme

Currently, this site uses the [[ https://github.com/frjo/hugo-theme-zen][Hugo Zen theme]], which I am modifying to make 'my
own'. The earlier theme was a modified version of a much older version of the
Hugo Academic theme. The main reason was to progressively reduce 'bloat' and
focus on content while also using a code base that was more approachable.

I am just another hacker (/both in skills and spirit/), and not any kind of expert
in web design. Constructive feedback is always welcome.

* CSS Customizations and Maintenance Notes

** Overview

This site uses a carefully orchestrated system of custom CSS files to achieve specific layout behaviors, particularly for post listings on archive and tag-filter pages. Understanding the CSS loading order and customization architecture is crucial for maintenance.

** CSS Loading Order

The CSS files are loaded in a two-tier system optimized for performance:

*** Global CSS (Loaded on ALL Pages)
Loaded via =layouts/partials/head/styles.html= in this order:

1. **Theme base styles** (from =themes/zen/assets/sass/styles.scss=)
   - Core Hugo Zen theme styles
   - Layout grid system, typography, base components
2. **=assets/css/elegant-clean.css=** (~743 lines)
   - Global overrides and customizations
   - Tag pill styling, post layout rules
   - Responsive navigation, mobile menu
   - Consolidated from former =responsive-nav.css=
3. **=assets/css/unified-pages.css=** (~135 lines)
   - Page header normalization across all page types
   - Title consistency (single, list, taxonomy pages)
   - Badge styling for list count
   - Width and padding normalization
   - **Purpose**: Ensures about, search, posts, projects, and tag pages have identical formatting

*** Page-Specific CSS (Conditionally Loaded)
Loaded only on relevant pages via template =define "head"= blocks:

1. **=assets/css/archive.css=** (~269 lines)
   - Loaded by: =layouts/_default/archives.html=
   - Archive page specific styling
   - Year navigation, compact post items
2. **=assets/css/tags-filter.css=** (~475 lines)
   - Loaded by: =layouts/tags-filter/single.html=
   - Tag filter page with search functionality
   - Interactive tag selection styling
3. **=assets/css/list-pages.css=** (~260 lines)
   - Loaded by: =layouts/_default/list.html= and =layouts/taxonomy/tag.html=
   - Post/item listing styles for projects, tags, categories
   - **Note**: Header, title, and badge styles are in =unified-pages.css= to avoid duplication
   - Focuses on post item layout, summaries, read-more links

*** Why Keep unified-pages.css and list-pages.css Separate?

**Performance Optimization Strategy:**
- **unified-pages.css** (135 lines): Loaded on ALL pages
  - Minimal size ensures fast load times
  - Only contains styling needed by every page type
  - Headers, titles, badges that appear universally

- **list-pages.css** (260 lines): Loaded ONLY on list pages
  - ~260 lines of list-specific CSS not needed on about, search, or post pages
  - Conditional loading aligns with site's 250kb.club optimization goals
  - Reduces CSS payload by ~50% on non-list pages

**Duplication Avoidance:**
- Header, title, prefix, and badge styles centralized in =unified-pages.css=
- =list-pages.css= references this in comments to prevent re-addition
- Mobile responsive rules for page headers live in =unified-pages.css=
- Only post item-specific responsive rules remain in =list-pages.css=

**Consolidation Considered But Rejected:**
- Merging would add 260 lines to every page load
- Current two-tier system provides better performance
- Page-specific CSS loading is a web performance best practice

** Key Custom CSS Files

*** =assets/css/unified-pages.css= (NEW - Added 2025)
- **Purpose**: Normalize formatting across all page types (single, list, taxonomy)
- **Key Features**:
  - Width and padding consistency for all pages
  - Header formatting (margins, borders) unified across page types
  - Title sizing standardized at 2rem (desktop) / 1.75rem (mobile)
  - Badge styling with 1rem gap from title text
  - Vertical centering of badge with heading using =align-items: center=
- **Covers**: About, search, posts, projects, tag listings, category listings
- **Loading**: Global (via =layouts/partials/head/styles.html=)
- **Maintenance**: Single source of truth for page headers and titles

*** =assets/css/elegant-clean.css=
- **Purpose**: Global styling overrides and base theme customizations
- **Critical Role**: Contains foundational styles but DELIBERATELY excludes post layout rules to avoid conflicts
- **Maintenance Warning**: This file previously contained conflicting =.post-single-line= rules that were removed to prevent layout conflicts. Do NOT re-add post layout rules here.

*** =assets/css/archive.css=
- **Purpose**: Archive page specific styling
- **Key Features**: 
  - Compact "Jump to Year" section with reduced padding
  - Compact year pills with smaller dimensions
  - Two-line post layout: Title+Date on line 1, Tags (indented) on line 2
  - No box styling around posts section
- **Layout Structure**: Uses =.post-first-line= and =.post-second-line= for hierarchy

*** =assets/css/tags-filter.css=
- **Purpose**: Tag filter page specific styling
- **Key Features**: Identical post layout to archive page for consistency
- **Layout Structure**: Mirrors archive.css post layout exactly

*** =assets/css/list-pages.css=
- **Purpose**: Post/item listing styles for project pages, tag pages, category pages
- **Loading**: Conditionally loaded by =list.html= and =tag.html= templates
- **Key Features**:
  - Post item layout (title, date, tags, summary)
  - Read More link styling
  - Archive-compatible post item structure
  - Mobile responsive post items
- **Dependencies**: Relies on =unified-pages.css= for headers, titles, and badges
- **Maintenance**: Do NOT add header/title/badge styles here - they belong in =unified-pages.css=

** Post Layout Architecture

The current post layout uses a two-line hierarchical structure:

#+BEGIN_SRC css
.post-single-line {
  display: flex !important;
  flex-direction: column !important;
  /* Creates vertical stacking of two lines */
}

.post-first-line {
  /* Title on left, Date on right */
  justify-content: space-between !important;
}

.post-second-line {
  /* Tags indented with 1rem left padding */
  padding-left: 1rem !important;
  justify-content: flex-start !important;
}
#+END_SRC

** Critical Maintenance Guidelines

*** CSS Conflict Prevention
1. **Never add** =.post-single-line= or =.post-tags= rules to =elegant-clean.css=
2. **Always use** =!important= declarations in page-specific CSS files to override theme defaults
3. **Maintain identical** post layout rules between =archive.css= and =tags-filter.css=

*** Template Structure
- Archive page: =layouts/_default/archives.html=
- Tag filter page: =layouts/tags-filter/single.html=
- Both use identical HTML structure with =.post-first-line= and =.post-second-line= classes

*** Layout Requirements
- **Line 1**: Post title (left) + Date (right)
- **Line 2**: Content type badge + Tags (left-aligned, indented 1rem for hierarchy)
- **No boxes**: Post sections should have no background/border styling

** Favicon Configuration

The site uses a comprehensive favicon setup:

- **Essential files**: =favicon.ico= (multi-size), =apple-touch-icon.png= (180x180)
- **Location**: =static/= directory
- **Configuration**: Set in =config.yaml= as =favicon: "favicon.ico"= and =icon: "favicon-32x32.png"=
- **Optimization**: Files are compressed and optimized for web delivery

** Troubleshooting

*** Tags Not Left-Aligned
- Check if =elegant-clean.css= has conflicting rules
- Ensure =justify-content: flex-start !important= is present in page-specific CSS
- Verify =!important= declarations are not being overridden

*** Layout Breaking on Updates
- Hugo theme updates may introduce conflicting CSS
- Always test archive and tag-filter pages after theme updates
- Check browser developer tools for CSS rule conflicts

*** Performance Considerations
- CSS files are minified and fingerprinted in production
- Favicon files are optimized but could be further compressed if needed
- Monitor total CSS payload size when adding new customizations

** Mobile Navigation and Responsive Design

*** Mobile Menu Configuration
- **Enabled**: =mobileMenu: true= in =config.yaml=
- **Behavior**: Automatically switches from sticky header menu to slide-out mobile menu on screens ≤666px
- **Implementation**: Uses Zen theme's built-in mobile menu with enhanced styling

*** Mobile Menu Fixes Applied
- **Toggle Visibility**: Fixed z-index conflicts (=z-index: 9999=) to ensure hamburger button appears above header
- **Header Positioning**: Set header to =position: static= on mobile to prevent interference
- **Unobtrusive Design**: 75vw width menu with backdrop blur and subtle overlay
- **Touch-Friendly**: 44px minimum touch targets and improved visual feedback

*** Responsive Navigation Features
- **Desktop**: Compact menu items with reduced padding (0.3rem 0.6rem)
- **Mobile**: Slide-out menu (75vw width) with backdrop blur and smooth animations
- **Toggle Button**: Fixed position top-right with glassmorphism styling
- **Sticky Header**: Only active on desktop; disabled on mobile to prevent conflicts
- **CSS File**: =assets/css/responsive-nav.css= handles all responsive behavior

*** Footnote Scroll Offset Fix
- **Problem**: Sticky header obscures footnote targets when clicked
- **Solution**: =scroll-padding-top: 80px= (desktop) / =60px= (mobile)
- **Features**: 
  - Footnote targets are highlighted with fade animation
  - Works with both forward and back footnote links
  - Responsive offset adjustments for different screen sizes

*** Navigation Maintenance Guidelines
- **Mobile Menu Toggle**: Fixed positioned at top-right with backdrop blur
- **Menu Item Spacing**: Controlled via =gap= and =padding= properties in =responsive-nav.css=
- **Scroll Offsets**: Adjust =scroll-padding-top= values if header height changes
- **Mobile Detection**: Uses CSS media queries (max-width: 666px) to match theme's JS breakpoint

* Recent Performance Optimizations (2025)

** Overview

The site has undergone significant performance optimizations to qualify for [[https://250kb.club][250kb.club]] standards while maintaining full functionality. These optimizations focus on reducing HTTP requests, minimizing file sizes, and improving mobile user experience.

** Performance Metrics

*** Before Optimization
- **CSS Files**: 6-7 separate CSS files per page
- **Total CSS Size**: ~74KB per page
- **Page Sizes**: 150-195KB (within 250kb.club limits)
- **HTTP Requests**: Multiple CSS file requests

*** After Optimization
- **CSS Files**: Reduced HTTP requests through strategic consolidation
- **File Size Reductions**: 25-81% reduction in favicon files, CSS cleanup
- **Enhanced Mobile Experience**: Improved tag pill styling and alignment
- **Maintained Functionality**: All features preserved while reducing bloat

** CSS Consolidation and Optimization

*** Files Merged and Consolidated

**** =responsive-nav.css= → =elegant-clean.css=
- **Action**: Merged 247 lines of responsive navigation CSS
- **Impact**: Eliminated one HTTP request per page load
- **Content**: Mobile navigation, footnote fixes, scroll-to-top positioning
- **Result**: =responsive-nav.css= removed from =layouts/partials/head/styles.html=

*** CSS Rule Cleanup in =elegant-clean.css=
- **Removed Unused Rules**: Tables, project/card styling, pagination
- **File Size Reduction**: 19.6KB → 17.7KB (9.7% reduction)
- **Fixed CSS Errors**: Removed invalid =rx: 2 !important;= SVG property causing console errors
- **Maintained**: All essential styling preserved

*** Page-Specific CSS Loading
- **Strategy**: Page-specific CSS files (=archive.css=, =tags-filter.css=) load only when needed
- **Implementation**: Already optimized with conditional loading
- **Benefit**: Avoids loading unused CSS on irrelevant pages

** Favicon Optimization

*** File Size Reductions
- **apple-touch-icon.png**: 11.7KB → 8.8KB (25% reduction)
- **android-chrome-192x192.png**: 54.7KB → 10.5KB (81% reduction)
- **Method**: Lossless compression and optimization for web delivery
- **Quality**: Visual quality maintained while significantly reducing file sizes

** Mobile User Experience Improvements

*** Tag Pills Mobile Optimization
**** Problem Solved
- Tag pills appeared too large and misaligned on mobile devices
- Content was aligned to top instead of center within pills
- Poor visual hierarchy on archive and tag-filter pages

**** Solution Implemented
***** General Mobile Tag Pills (=elegant-clean.css=):
#+BEGIN_SRC css
@media (max-width: 768px) {
  .tag-pill, a.tag-pill {
    padding: 0.25rem 0.5rem !important;
    font-size: 0.7rem !important;
    min-height: 28px !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
  }
}
#+END_SRC

***** Post Tags Specific Mobile Styling:
#+BEGIN_SRC css
.post-tags .tag-pill {
  padding: 0.15rem 0.35rem !important;
  font-size: 0.65rem !important;
  min-height: 22px !important;
  line-height: 1.2 !important;
}
#+END_SRC

***** Page-Specific Mobile Enhancements:
- **=tags-filter.css=**: Added mobile-specific tag alignment rules
- **=archive.css=**: Added mobile-specific tag alignment rules
- **Result**: Consistent mobile tag pill experience across all pages

** Tag Search Functionality Enhancement

*** Issues Resolved

**** Search Not Working
- **Problem**: Tag search input had no visual effect on available tags
- **Root Cause**: CSS specificity conflict - =.tag-pill { display: inline-block }= overriding JavaScript's =display: none=
- **Solution**: Used =setProperty('display', 'none', 'important')= to force style application

**** Search Algorithm Improvements
- **Original**: Basic substring matching
- **Enhanced**: Multi-priority semantic search algorithm

***** Search Matching Priorities:
1. **Exact word match** at word boundaries
2. **Word prefix match** for CamelCase words  
3. **Substring match** (3+ characters only)
4. **Tag prefix match**
5. **Acronym matching** (2+ characters)
6. **Fuzzy matching** with word boundary detection

***** Features:
- **Multi-word support**: "py data" matches "PythonDataAnalysis"
- **CamelCase aware**: Splits "ProductivityJournal" into ["productivity", "journal"]
- **Case insensitive**: All matching is case-insensitive
- **Length restrictions**: Prevents false positives (e.g., "py" no longer matches "ProductivityJournal")

*** Implementation Details
#+BEGIN_SRC javascript
// Priority-based semantic search in tags-filter.js
shouldShow = searchWords.every(word => {
  const camelCaseWords = originalTagName.split(/(?=[A-Z])/).map(w => w.toLowerCase());
  // Priority 1: Exact word match
  if (camelCaseWords.some(w => w === word)) return true;
  // Priority 2: Word prefix match  
  if (camelCaseWords.some(w => w.startsWith(word))) return true;
  // Additional priorities with length restrictions...
});
#+END_SRC

** Technical Debt Fixes

*** CSS Console Errors
- **Error Fixed**: "Error in parsing value for 'rx'. Declaration dropped."
- **Cause**: Invalid CSS property =rx: 2 !important;= meant for SVG elements
- **Location**: =elegant-clean.css:571=
- **Resolution**: Removed invalid property, error eliminated

*** CSS File Organization
- **Consolidated**: Merged related CSS files to reduce HTTP requests
- **Maintained**: Logical separation between global and page-specific styles
- **Optimized**: Removed unused CSS rules without affecting functionality

** 250kb.club Compliance Status

*** Current Status: ✅ QUALIFIED
- **Page Sizes**: Consistently under 250KB limit
- **Optimization Level**: Significant improvements made while maintaining full functionality
- **Future Considerations**: Additional optimizations possible but not necessary for compliance

*** Optimization Strategies Applied
1. **HTTP Request Reduction**: Merged CSS files where logical
2. **File Size Optimization**: Compressed images and cleaned unused CSS
3. **Mobile-First Approach**: Enhanced mobile experience without desktop compromise
4. **Selective Loading**: Page-specific CSS only loads when needed
5. **Performance Monitoring**: Established baseline for future optimizations

** Maintenance Guidelines for Performance

*** CSS File Management
- **Never re-separate** merged CSS files without performance justification
- **Monitor total CSS payload** when adding new customizations
- **Test mobile experience** thoroughly after any CSS changes
- **Use browser dev tools** to verify HTTP request counts

*** Mobile Optimization
- **Test tag pills** on actual mobile devices, not just browser resize
- **Maintain consistent** mobile styling across archive and tag-filter pages  
- **Verify center alignment** of tag content on various screen sizes
- **Check touch targets** meet accessibility guidelines (44px minimum)

*** Search Functionality
- **Preserve search algorithm** complexity - simpler algorithms cause user frustration
- **Test edge cases** like single-character searches and special characters
- **Maintain CamelCase awareness** for technical tag names
- **Debug with console logging** if search behavior changes unexpectedly
